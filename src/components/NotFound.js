// react
import React from "react"

const NotFound = () => {
  return (
    <p
      style={{
        position: "fixed",
        top: 0,
        left: "30rem",
        right: 0,
        bottom: 0,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "2rem",
        fontWeight: 300,
        color: "#999"
      }}
    >
      Page not found.
    </p>
  )
}

export default NotFound
